ブランチ名って接頭辞つけた方がいいのか

git log --oneline -5 で直近のコミット一覧表示できる(5って何？)

git checkout -b ブランチ名 別のブランチから新しいブランチを作成&移動

一回使ってすでにマージしたブランチは再度使わない。新しいもの作って古いものは捨てなさい

思いがけずちょうどよく繋がることもあるんだな😄
（例：このタスクをやったら残業がなん時間増える、ではなく、タスクの所要時間を持たせることにした→→AIに気付かされたが、「スロット制」(2時間おきに進む)と相性がいいようです。(1タスク=1ターン=2時間、と決まっているなら、「タスクの所要時間」もこの2時間という単位で自然に扱えます)）

なかなかテーブル作れずに概念設計に時間がかかっている、これがいいことか悪いことかがわからない。ただ遅い。

正規化よりもER図？
→ER図が概念設計で、正規化が論理設計なので、一応ER図が先だそうです。

一人一回しかプレイできないと決めていたらしいけど覚えていなかった。claudeが教えてくれた。なんでそんなこと言ったんだろう。

gameplayをユーザーに結びつけるのと、タスクに結びつけるのと何が違うんだろう。
→いやgameplayの中に利用者IDあるのになんでだ？混乱したからtableに戻る。
→gameplayの中にFKの利用者idがあるが、そもそもuserとtaskが1対多の関係がだからかもしれない。
→user(多のやつ)とtaskの間に中間テーブルを作るのは？(problemsへ)

git stash -u → 未追跡ファイルも含めて一時退避
git stash pop → 退避したファイルを戻す
git rm --cached → ローカルには残してGit管理から外す

Q. git commit やpushするときの自分がいるフォルダの位置って重要？
git add . → 今いる場所が重要 
git commit → addされた変更を記録する 
git push → ブランチのコミットをGitHubへ送る

git add -A は、ファイル移動をGitに「次のコミットに入れる変更」として登録するコマンド。

SSHではなくhttpでGithubに接続したので、鍵認証がいらなかった。
branch消すなら2段階で消さなくてはいけない。(github上とローカル※mac/コンテナ上)
・github: git push origin --delete ブランチ名
・ローカル: git branch -d ブランチ名

保留していたやつ分かったかもしれない
ユーザー管理だけにしてしまうと、gameplayの中のtaskのステータスは利用者ごとに変えられているため、また同じ利用者がログインしてしまうと前のステータスが反映されるようになってしまう。
そのため、gameplayとtaskの中間にステータス管理のエンティティを作成し、ユーザーごとではなくgameplay(1ゲーム)で管理することで、同じユーザーでもプレーするごとに新しいステータスを持てるということ？
→claude: そうです。

ER図がおかしい。ステータス管理エンティティにゲーム状況idあるのに、なんでゲーム状況都戦で繋げていないんだ？？
 →あ、いや、つながっていました（？）
gameplayにovertimelog_idを繋げて残業記録をゲーム状況に反映できると思っていたが、確認してもらったら違うらしい。
→→claude: 外部キーは「多」側が「１」側を指す
GamePlay(ゲームの状況)
  id, user_id(FK), OvertimeLog_id(FK), player_status(ユーザーステータス: プレイ中/死亡/生存クリア)
こう書いていたが、これはダメらしい、残業状況が多のため。(OvertimeLogのidではなく、GameplayのidをFK(OvertimeLogエンティティ内))

migrate→generate