- <form>タグをつけないとボタンがただの飾りになってしまう
- passwordは、typeでpasswordを指定すると、⚫︎⚫︎⚫︎とかくれる仕様にしてくれる
- CSSは一旦後にする
- テキストフィールド見えないので修正
- ログインの際に、重複した名前があると自分のアカウントかどうかが怪しいので、user_nameにユニークをつける(findFirstだと同じユーザー名の別アカウントにログインしてしまうかもしれない)
- findUniqueで登録ユーザーを探すときにはwhereで名前とパスワードの両方を指定しなければ名前さえ合ってればログインできてしまうことになる
- →prismaでは2つ組み合わせた検索条件はできないらしい。findFirstなら複数条件を組み合わせてANDで絞り込むことができる
- <form>タグにaction={loginUser(asyncの関数名)}を指定しなければ、関数が呼び出されないので書きます
- claudeが使えなくなったタイミングでわかる範囲のcss追加(className)と作成するページの構造を詳しく考える。テキストやwebサイトを参考に。(0914 13:00)
- yupはuse server関数の中でいいとのことですがいかがでしょうか
- →調べたところ、前にも出てきていたがzodというバリデーションのライブラリがあるそうで、type scriptのために作られた簡単な型の自動抽出があるらしいのでバリデーションは後回しにします。
- スタート画面に入る(0914 14:23)。
- includeを使う。icludeはテーブル名ではなく、リレーション名。※gameplaydetail     GamePlayDetail[]　こんな感じのやつ。
- .mapに<tr>keyをつけ忘れていた
- modelを変更して、migrate devを実行した後もts側に反映させるにはgenerateが必要らしい。→バージョンが新しくなり、generateが自動で実行されなくなった
- 新規会員登録画面作成(091416:02) ※ログイン画面はなんとなく作成完了。
- upsertというもしなければ登録、あれば更新するというメソッドがあったので、それを新規会員登録で使えそうとのこと
- 新規会員登録までは一旦書籍登録などテキストに書いてあるものにちかい構造なので、最初は自分で書いてエラーになってどうしても治せない時はclaudeに聞いてやった
- 一旦会員登録系は区切りがついたので次にタスク系にいこう(0914 17:06)　※feature/task-management
- formData.get()は必ずstring型で返ってくるのでInt型にしてDBへ送らないとエラーなる(Number()を使う)
- findUniqueはunique制約ついていないと使えませんので、findFirst使います
- 新しいタスクを作るにはuser_idが必要→urlで受け取る必要がある。※addtask/[userId]/page.tsx
- userIdのタスク一覧ページのURLをページを作成したら編集する
- タスク一覧はユーザーなくてもいいか、(追加以外はなくてもいいか)と思っていましたが、そもそもタスクはログイン後でないと見ることはできない。なので、タスク関連は全てユーザを取得しないといけないかもしれない。
- →ログイン後でしかみられない、とURLにuserIdを含めるかどうかは別の話らしい。編集削除はtask_idがあればできる。しかし、追加・一覧は誰のタスクを表示/追加するのかがわからないといけない。
- フォルダ構成まとめについては、ページ名がフォルダ名になるので、ダメだった。忘れてた
- startの[id]ってユーザーidかと思っていたのですが、[userId]とは何が違うんですか？
- →名前が違うだけで中身は同じ。startの方をuserIdに変えることも可能。
- タスクってuserIdごとのタスク？編集はidで、という話でしたが、これはuserIdなのではないなら、編集・削除されたタスクは他のユーザーにも反映されるってことですか？今のままでは”問題”がある。
- 1. あなたが /task/5/edit を開いて、自分のタスクを編集する
- 2. 別の人(悪意があるかどうかは関係なく)が、
-    ブラウザのURL欄に直接 /task/8/edit と打ち込む
- 3. task_id=8 が、たまたま「あなたの」タスクだったとする
- 4. その人は、あなたのタスクを、勝手に編集・削除できてしまう
- →ログインしているか(認証)と、この人が操作していいか(認可)の異なる仕組みが必要。※バックエンド
- →簡易的に/task/[userId]/[taskId]/editというようにURLに含めてserver actionの中でチェックすることもできる。
- →実務ではおそらくバックエンドでやると思うので、時間があれば理解する。今回の実装では後回しにする。(詳しくは要件定義書の「既知の課題」に記述)
- なぜかDBに登録済みのuserで/loginにてログインしてもstartとuseridがしっかりURLに出ているのにも関わらず404になるのは、loginのredirectのルートが違かったから
- パスワードのなんちゃらが検出されました。とでました。おそらくブラウザのデフォルト機能の何かですが、これはこちらで操作できるんかな
- クエリパラメータとルートパラメータの違い
- →クエリにははてな?がついている。受け取るのはsearchParams。ルートはフォルダ自体に[id]をつけている。受け取るのはparams。
- 2つも方法があるのはなぜか、その情報がそのページの本質的な一部かどうか。
- ・ルートパラメータはそのページ自体を特定する必須の情報
- ・クエリは同じページの中のオプション的な条件
- 補足: クエリは絞り込みや並べ替え。同じページの中で、表示条件だけを変える」時に向いている。
- alltaskのpage.tsxに仮の名前でplaygameのリンクをおいているので要チェック
- 編集はユーザーidが必要になるので、ルートパラメータが必要だが、編集削除はいらないらしい
- タスク一覧見るのにパラメータが必要ないのはなんでだろう、勝手にログインしているらしいけど、どういうことだかがわからない。裏でログイン機能があるならパラメーターなんて使う意味ある？→cookieとsessionの機能を使っていればの話でした。今の実装はURLのuserIdと本当にログインしているユーザーが同一人物であるかどうかを確認する仕組みはないとのこと。
- 今の”問題点”は、urlを書き換えて仕舞えば他の人でもuserIdでタスクを編集できてしまうこと
- Link href="/addtask/[userId]ではなく、Link href={`/addtask/${userId}`こういう書き方をしないと動作しません。(ずっとダブルクォーテーションと{)なしで書いてしまっていた)
- ログイン画面に新規会員登録がまだの方のリンクもはりたい
- コード直しに夢中になって次に何するかわからなくなった→claudeに聞いたら編集画面の削除と編集機能でした。
- clientコンポーネントを作っていないのにボタンやリンクを押したら反応してくれるのはbuttonやlinkに属性がついているからですか？ブラウザ側の操作ってserver componentでは直接感知できないと思っていました
- →リンクをクリックして別のURLへ移動すること自体は、ブラウザがもともと持っている機能だから(?)つまり、クリックをServer Componentが感知しているわけではありません。(?)
- →リンクやフォームを置くことはできるが、ブラウザ上の細かい操作を直接操作するわけではないらしい。本当によくわかりません。もう少しわからないところがわかってきたらまた質問します※落ち着いたらまた考え直す
- 次は削除と編集ボタンを追加(0915 15:53)
- <tr>の中に<div>入れてはいけない
- もしタスクがなかったらありませんで404するんじゃなくて、ありませんというバリデーションをつけたい(edit/ page.tsx)
- 違うパラメーター？のフォルダ直下のファイルに別のパラメータ直下のファイルの遷移先のuRLを使いたい時、どうすればいいかわからないのでedit page.tsxの遷移先を確認してください→`/alltask/${task.user_id}`らしい
- if(!task) notFound();で、taskがnullではない、とその関数で教えてくれている
- <tr>の中に<Link>を置くとエラーなる　※<td>タグで囲めばOK
- deletetsxは部品なので[userId]の下に置かなくても関係ない
- 第31章で、プライベートフォルダというものがあったので、削除ボタン部品を_componentsフォルダに入れました。(こロケーションというらしいです)
- if(!task) notFound();　がeditの中で2つあるのは関数が2つあるから。
- ※また、1つ目はfindUniqueがある。指定したidを直接return文の中で                   defaultValue={task.task_name}　例えばこのように使うとなると、もし指定したidでなかった場合にnullの可能性があるものを直接使おうとしてエラーになるので、どちらの関数でも必要である
-  / がなしだと相対パスらしい（絶対パスは階層の一番上から、相対パスはカレントディレクトリからの道順）よくわかっていない、コンポーネントの中でも気にした方がいい理由とは？
- 削除ボタン後にしても良かったかもしれない
- 一旦タスク系区切り(0916 10:55)　ゲームへ ※feature/gameplay
- startページにゲーム始めるボタンを追加してからページ作成しよう
- icludeなんだっけな(startの中)　振り返ったらわからないと思った→関連するテーブルを一緒に取得するやつ()
- server actionをいちいちどこで作るか迷う。(削除機能の時とか、ゲームスタートの時とか)
- ※その動作を行う処理を、どこで実行されたいか、を考えるそう、よくわからないけど
- ゲームを始める処理で、スタート画面でgameplay idを作ってからそのidのページに遷移するか、遷移さきでそのGamePlayがまだなければ作る、という2つ方法があるそう、テキストは前者。
- ゲーム開始のリンク移動について。redirectがstartGamePlay関数の中にあるので、Linkタグではなく、formタグでsubmitを使う。submitでcreateしたgameplayをredirect先に送っている？→少し違うらしい
- redirect 関数自体が、ブラウザに次にどのURLへ移動してほしいかを指示しているとのこと。
- 時間が詰まっているので、ログイン機能も後にすれば良かったのかもしれません。
- gameplay.pageのincludeはどうするべきか？ゲーム詳細とタスクをjoinさせるべきかな？
- →まだやっていないタスクをどう探すかという問題にぶち当たります。(タスクにはステータスはもたせていない)
- gameplaydetailには完了したタスクしか入っていない。まだ選べるタスクはtaskにある。
- ※まだ選べるタスクだけを表示するには、task - gameplay　をしなくてはいけない！そのため、2つのテーブルを比べなければならないので必要。
- 56番のincludeの書き方に難航中(claudeが使えないのでテキストで自力で頑張る)
- 現在14時。後１時間でマージの締切のため、区切りを見つける。gameplayのリンクpageを作成し、gameplayIdを探す処理まで書きました。
- ゲームとなったらどんな処理を書けばいい？ロードマップなので、タスクのランダム表示・ポイント計算・プレイヤーステータスの変更の有無処理・残業時間計算処理を書いて、それを順番に並べればいいのかな。22時判定はどうする？10時始業にして、2時間ごとに進むので6ターン。タスクの所要時間を確認して6ターン処理を繰り返すようにしないといけないですよね？
- 処理のコードを書く前に、しばらく日本語で整理したいので一旦マージをします。→ 渡邊さんへ提出