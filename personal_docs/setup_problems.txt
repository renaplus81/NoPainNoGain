環境構築の
docker run --rm -it -v "$PWD":/app -w /app node:22-trixie-slim \
  bash -c "corepack enable && pnpm dlx create-next-app@latest . \
    --typescript --eslint --tailwind --app --src-dir \
    --import-alias '@/*' --use-pnpm --skip-install"
この時点でエラーが出るのは詰みだわ。折れそう。next.jsの一番最初なのに
無理や、できない、1時間半くらいここで止まってます。今15:25。(14:00~)
chat gptとclaudeに聞いても何回も同じコード書かされるの

→今渡邊さんに聞いている
docker run --rm -it \
-v "$PWD":/app \
-w /tmp \
node:22-trixie-slim \
bash -c "corepack enable && pnpm dlx create-next-app@latest /app \
--typescript --eslint --tailwind --app --src-dir \
--import-alias '@/*' --use-pnpm --skip-install"

aiの使い方がダメなのか。なんで出来なかったんだろう


----------------------------
今度はコンテナ開いたらどうやらDNSエラー。
エラーばっかりなんでーー→→別の研修ファイルもコンテナで開けない。
コードの問題ではないかもしれない
「DNSがおかしい」というだけではなく、コンテナから外部ネットワークへ出ていくところ自体に問題があるとのことです(chatgpt)
docker desktopのアップデートや再起動を試してみる。
→アップデートしたらいけたぞ！
Dockerコンテナは動いているけど、VS Codeがそのコンテナに接続できない、という問題だった。

→→一旦ローカルに戻ってsetupのブランチを綺麗にする。
(thelastproject直下に環境構築フォルダ・ファイルズをおきたい)
→綺麗になって、もう一度デブコンテナで開こうとしたらエラーやないかい

ポートの衝突が起きていたので、直した。だが、またDNSがおかしい。
→100% ネットワーク環境（Dockerとパソコンの間の設定）の問題で、コード間違いではないそう。Docker全体の設定にパブリックDNS（8.8.8.8）を追加する、ということを指示されたので、後でそれをやった方がいいか渡邉さんに確認してみるが、その前にもう一回rebuildしてみる。

→え、なんかDNSの問題がなくなったんだけど。なにもしていないよ？
プンプンデゔします。成功！
❓DNS名前解決が失敗していたが、Rebuild後には正常に戻った。なぜ戻ったのかはまだ特定できていない(※名前解決とは、人間が覚えやすい「ドメイン名」を、コンピュータが通信に使う「IPアドレス」に変換する仕組み)

dockerの内部DNSとコンテナのネットワークのやりとり？もそんな感じなのかな、サーバーとかよくまだ理解できていない。

SSHではなくhttpでGithubに接続したので、鍵認証がいらなかった。(noticeにも書く)

----------------------------
ここからprisma(nextjsでおよそ8時間かかりました。)
問題なく、prisma環境構築のテキスト手順5まで終わりました。
